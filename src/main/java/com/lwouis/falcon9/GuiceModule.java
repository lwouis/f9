package com.lwouis.falcon9;

import com.google.inject.AbstractModule;

class GuiceModule extends AbstractModule {
  @Override
  protected void configure() {
    // Bindings are done through annotations
  }
}